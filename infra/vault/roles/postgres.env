# postgres — Olympus VM. TLS leaf only; the postgres password stays in .env.
ROLE=postgres
COMMON_NAME=postgres.bgalhardo.internal
IP_SAN=192.168.1.177
DEPLOY_DIR=/root   # this VM deploys to /root, not /root/postgres
